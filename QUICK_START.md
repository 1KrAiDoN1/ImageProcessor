# 🚀 Quick Start - ImageProcessor

Самый быстрый способ запустить ImageProcessor и начать работу.

## ⏱️ За 2 минуты

### Шаг 1: Установите Docker

Если еще не установлен:
- **macOS**: [Docker Desktop](https://www.docker.com/products/docker-desktop)
- **Windows**: [Docker Desktop](https://www.docker.com/products/docker-desktop)
- **Linux**: 
```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
```

### Шаг 2: Клонируйте репозиторий

```bash
git clone <repository-url>
cd ImageProcessor
```

### Шаг 3: Запустите все сервисы

```bash
# Один из способов:

# Вариант 1: Makefile (рекомендуется)
make docker-up-all

# Вариант 2: Docker Compose напрямую
docker-compose up -d

# Вариант 3: Скрипт
./scripts/start.sh  # если есть
```

### Шаг 4: Подождите 15-20 секунд

Сервисы запускаются и проверяют готовность друг друга.

Проверить статус:
```bash
docker-compose ps
```

Все сервисы должны показывать статус "Up (healthy)".

### Шаг 5: Откройте браузер

Перейдите на: **http://localhost**

## 🎉 Готово!

Теперь вы можете:

1. **Загрузить изображение**: Перетащите или выберите файл
2. **Выбрать операции**: Миниатюра, изменение размера, водяной знак
3. **Обработать**: Нажмите "Обработать изображение"
4. **Посмотреть результаты**: Получите обработанные версии

## 📍 Доступ к сервисам

После запуска доступны:

| Сервис | URL | Описание |
|--------|-----|----------|
| 🌐 Frontend | http://localhost | Веб-интерфейс |
| 🔌 API | http://localhost:8080 | REST API |
| 📦 MinIO Console | http://localhost:9001 | S3 хранилище (minioadmin/minioadmin) |
| 🗄️ PostgreSQL | localhost:5432 | База данных (postgres/postgres) |
| 📨 Kafka | localhost:9092 | Брокер сообщений |

## 🧪 Проверка работы

### Проверить API

```bash
curl http://localhost:8080/health
```

Должен вернуть:
```json
{
  "status": "healthy",
  "service": "image-processor-api",
  "timestamp": "2026-02-02T..."
}
```

### Проверить Frontend

Откройте http://localhost - должна загрузиться страница с логотипом.

### Проверить MinIO

1. Откройте http://localhost:9001
2. Логин: `minioadmin`
3. Пароль: `minioadmin`
4. Должна быть видна консоль MinIO

## 📝 Первый запрос через API

### Upload изображения

```bash
curl -X POST http://localhost:8080/api/v1/images \
  -F "image=@/path/to/your/image.jpg" \
  -F 'operations=[{"type":"thumbnail","parameters":{"size":200}}]'
```

### Get статус

```bash
curl http://localhost:8080/api/v1/images/{IMAGE_ID}/status
```

### Get изображение

```bash
curl http://localhost:8080/api/v1/images/{IMAGE_ID}?operation=thumbnail \
  --output result.jpg
```

## 🛑 Остановка сервисов

```bash
# Остановить все
docker-compose down

# Или с Make
make docker-down-all

# Остановить и удалить все данные (ВНИМАНИЕ!)
docker-compose down -v
```

## 🔄 Перезапуск

```bash
# Перезапустить все
docker-compose restart

# Перезапустить конкретный сервис
docker-compose restart api
docker-compose restart frontend
```

## 📊 Просмотр логов

```bash
# Все логи
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f frontend
docker-compose logs -f api
docker-compose logs -f worker

# С Make
make docker-logs-all
make docker-logs-frontend
```

## ❓ Проблемы?

### Порт уже занят

**Ошибка**: `port is already allocated`

**Решение**:
```bash
# Найти процесс на порту
lsof -i :80
lsof -i :8080

# Остановить или изменить порт в docker-compose.yaml
```

### Сервисы не запускаются

**Решение**:
```bash
# Проверить логи
docker-compose logs

# Пересобрать образы
docker-compose build --no-cache
docker-compose up -d
```

### Недостаточно памяти

**Решение**:
- Увеличьте memory для Docker Desktop
- Или уменьшите количество workers в docker-compose.yaml

### CORS ошибки

**Решение**: CORS уже настроен, но если проблема осталась:
1. Проверьте что используете http://localhost (не 127.0.0.1)
2. Очистите кеш браузера
3. Проверьте логи API: `docker-compose logs api`

## 📚 Дальше

После успешного запуска:

- 📖 [README.md](README.md) - Полная документация проекта
- 🐳 [DOCKER_SETUP.md](DOCKER_SETUP.md) - Детальная Docker документация
- 🚀 [DEPLOYMENT.md](DEPLOYMENT.md) - Развертывание в production
- 🎨 [frontend/USER_GUIDE.md](frontend/USER_GUIDE.md) - Руководство пользователя
- 💻 [frontend/DEVELOPMENT.md](frontend/DEVELOPMENT.md) - Разработка фронтенда
- 📋 [API_EXAMPLES.md](API_EXAMPLES.md) - Примеры использования API

## 💬 Помощь

Если что-то не работает:

1. Проверьте [DOCKER_SETUP.md](DOCKER_SETUP.md) - раздел "Решение проблем"
2. Посмотрите Issues в репозитории
3. Создайте новый Issue с описанием проблемы и логами

## 🎉 Наслаждайтесь!

Теперь вы готовы использовать ImageProcessor для обработки изображений!

---

**Время запуска**: ~2 минуты  
**Сложность**: Легко  
**Требования**: Только Docker

