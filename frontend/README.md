# Frontend - Image Processor

Современный веб-интерфейс для сервиса обработки изображений.

## 🎨 Особенности дизайна

- **Современный градиентный дизайн** с использованием фиолетовых оттенков
- **Адаптивная верстка** - работает на всех устройствах
- **Плавные анимации** и переходы
- **Drag & Drop** для загрузки файлов
- **Модальные окна** для просмотра изображений
- **Toast уведомления** для обратной связи
- **Real-time обновления** статуса обработки

## 📁 Структура

```
frontend/
├── index.html          # Главная страница
├── css/
│   └── style.css       # Стили приложения
├── js/
│   ├── api.js          # API клиент
│   ├── ui.js           # UI утилиты
│   └── app.js          # Основная логика
└── README.md
```

## 🚀 Запуск

### Простой способ (Python)

```bash
cd frontend
python3 -m http.server 8000
```

Откройте браузер: http://localhost:8000

### С помощью Node.js

```bash
cd frontend
npx http-server -p 8000
```

### С помощью Live Server (VS Code)

1. Установите расширение "Live Server"
2. Откройте `index.html`
3. Нажмите "Go Live" внизу

## 🔧 Конфигурация

API URL настраивается в `js/api.js`:

```javascript
const API_BASE_URL = 'http://localhost:8080/api/v1';
```

Измените URL, если ваш backend работает на другом адресе.

## 📱 Функционал

### Загрузка изображений

1. **Drag & Drop**: Перетащите изображение в область загрузки
2. **Клик**: Нажмите кнопку "Выберите файл"
3. **Выбор операций**: Выберите одну или несколько операций
4. **Настройки**: Настройте параметры для каждой операции
5. **Загрузка**: Нажмите "Обработать изображение"

### Поддерживаемые операции

- **Миниатюра**: Создание превью заданного размера
- **Изменение размера**: Масштабирование изображения
- **Водяной знак**: Добавление текста на изображение

### Галерея

- Просмотр всех загруженных изображений
- Фильтрация по статусу
- Поиск по имени файла
- Пагинация
- Клик для увеличенного просмотра

### Статистика

- Общее количество загруженных изображений
- Количество обработанных изображений
- Общий объем данных
- Среднее время обработки
- Детальная статистика по каждой операции

## 🎨 Кастомизация

### Цвета

Основные цвета определены в CSS переменных в `style.css`:

```css
:root {
    --primary: #667eea;
    --secondary: #764ba2;
    --success: #43e97b;
    --danger: #f5576c;
    --warning: #ffa726;
    --info: #4facfe;
}
```

### Компоненты

Все компоненты используют модульную структуру и могут быть легко изменены:

- Кнопки: `.btn`, `.btn-primary`, `.btn-success`, и т.д.
- Карточки: `.stat-card`, `.operation-card`, `.result-card`
- Модальные окна: `.modal`, `.modal-content`
- Toast: `.toast`, `.toast.success`, `.toast.error`

## 🔍 API Интеграция

### Методы API

```javascript
// Загрузка изображения
await api.uploadImage(file, operations);

// Получение статуса
await api.getImageStatus(imageId);

// Получение списка изображений
await api.listImages(limit, offset);

// Получение статистики
await api.getStatistics();

// Удаление изображения
await api.deleteImage(imageId);
```

### Polling статуса

```javascript
await api.pollImageStatus(imageId, (status) => {
    console.log('Progress:', status.progress);
}, 2000, 60);
```

## 🎯 Требования

- Современный браузер с поддержкой ES6+
- JavaScript включен
- Доступ к backend API (по умолчанию http://localhost:8080)

## 🐛 Отладка

### Включить логи

Откройте консоль браузера (F12) для просмотра логов:

```javascript
// Все API вызовы логируются
console.log('API call:', method, url);
```

### Проверка подключения

```javascript
// Проверить здоровье API
const isHealthy = await api.healthCheck();
console.log('API is healthy:', isHealthy);
```

## 📝 Примеры использования

### Загрузка с миниатюрой

```javascript
const operations = [{
    type: 'thumbnail',
    parameters: {
        size: 200,
        crop_to_fit: true
    }
}];

const result = await api.uploadImage(file, operations);
```

### Изменение размера с водяным знаком

```javascript
const operations = [
    {
        type: 'resize',
        parameters: {
            width: 1024,
            height: 768,
            keep_aspect: true
        }
    },
    {
        type: 'watermark',
        parameters: {
            text: '© My Company',
            opacity: 0.5,
            position: 'bottom-right'
        }
    }
];

const result = await api.uploadImage(file, operations);
```

## 🚨 Обработка ошибок

Все ошибки отображаются через toast уведомления:

```javascript
try {
    await api.uploadImage(file, operations);
} catch (error) {
    UI.showToast('Ошибка загрузки: ' + error.message, 'error');
}
```

## ⚡ Производительность

- **Lazy loading** для изображений в галерее
- **Debounce** для поиска
- **Polling оптимизация** - автоматическая остановка при завершении
- **Кеширование** presigned URLs

## 🔐 Безопасность

- Валидация типов файлов на клиенте
- Проверка размера файла (32MB max)
- Санитизация пользовательского ввода
- CORS настроен на backend

## 📞 Поддержка

При возникновении проблем:

1. Проверьте консоль браузера (F12)
2. Убедитесь, что backend запущен
3. Проверьте URL API в `api.js`
4. Проверьте CORS настройки

## 🎉 Приятного использования!

Наслаждайтесь красивым и функциональным интерфейсом для обработки изображений!

