# Quick Start Guide

Быстрый старт для ImageProcessor Frontend

## ⚡ За 60 секунд

### 1. Запустите Backend

```bash
cd /path/to/ImageProcessor
docker-compose up -d
```

Подождите 10-15 секунд, пока сервисы запустятся.

### 2. Запустите Frontend

```bash
cd frontend
python3 -m http.server 8000
```

### 3. Откройте браузер

Перейдите по адресу: **http://localhost:8000**

### 4. Загрузите изображение

1. Перетащите изображение в окно браузера
2. Выберите операцию (например, "Миниатюра")
3. Нажмите "Обработать изображение"
4. Готово! 🎉

## 🔧 Альтернативные способы запуска

### С помощью Node.js

```bash
cd frontend
npx http-server -p 8000
```

### С помощью npm

```bash
cd frontend
npm install
npm run serve
```

### С помощью Make

```bash
make frontend
```

## ✅ Проверка работоспособности

### Backend работает?

Откройте в браузере или выполните в терминале:

```bash
curl http://localhost:8080/health
```

Должен вернуть `200 OK`

### Frontend доступен?

Откройте: http://localhost:8000

Должна загрузиться страница с логотипом ImageProcessor.

## 🐛 Быстрое решение проблем

### Backend не работает

```bash
# Проверить логи
docker-compose logs api

# Перезапустить
docker-compose restart api
```

### CORS ошибка

Обновите `backend/internal/http-server/http-server.go`:

```go
import "github.com/gin-contrib/cors"

// В setupRoutes
router.Use(cors.New(cors.Config{
    AllowOrigins:     []string{"http://localhost:8000"},
    AllowMethods:     []string{"GET", "POST", "PUT", "DELETE"},
    AllowHeaders:     []string{"Content-Type"},
    AllowCredentials: true,
}))
```

Затем:

```bash
docker-compose restart api
```

### Порт 8000 занят

Используйте другой порт:

```bash
python3 -m http.server 8001
```

И откройте: http://localhost:8001

## 📱 Мобильная версия

Для тестирования на мобильных устройствах:

1. Узнайте IP адрес:
```bash
# macOS/Linux
ifconfig | grep "inet "
# Windows
ipconfig
```

2. Откройте на телефоне:
```
http://YOUR_IP:8000
```

Например: `http://192.168.1.100:8000`

## 🎯 Первые шаги

После запуска попробуйте:

1. **Загрузить изображение** - Drag & Drop или "Выберите файл"
2. **Создать миниатюру** - Выберите операцию и обработайте
3. **Открыть галерею** - Посмотрите загруженные изображения
4. **Проверить статистику** - Посмотрите информацию об обработке

## 📚 Дополнительная информация

- [README.md](README.md) - Полная документация
- [USER_GUIDE.md](USER_GUIDE.md) - Руководство пользователя
- [DEVELOPMENT.md](DEVELOPMENT.md) - Для разработчиков
- [EXAMPLES.md](EXAMPLES.md) - Примеры и тестирование

## 💡 Совет

Откройте DevTools (F12) чтобы видеть API запросы и возможные ошибки.

## 🎉 Готово!

Теперь вы готовы использовать ImageProcessor!

Наслаждайтесь обработкой изображений! 🚀

